@model YourProjectName.Models.Blog
@{
    ViewData["Title"] = Model.Title;
    ViewData["Description"] = Model.ShortDescription != null ? (Model.ShortDescription.Length > 160 ? Model.ShortDescription.Substring(0, 157) + "..." : Model.ShortDescription) : "Şevval Emlak Blog'da gayrimenkul dünyasına dair detaylı yazılar.";
    Layout = "_Layout";
    ViewData["HideNavbar"] = true;
    ViewData["HideFooter"] = true;

    // Admin olarak görünecek ek e-posta adresleri
    var additionalAdmins = new List<string> { "sftumen41@gmail.com", "exdel.txt@gmail.com" };
    var isAdminOrAdditionalAdmin = User.IsInRole("Admin") || (User.Identity.IsAuthenticated && additionalAdmins.Contains(User.Identity.Name));
}

@section Styles {
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts - Poppins -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- İkonlar için -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <!-- DetailsBlog.css -->
    <link rel="stylesheet" href="~/css/DetailsBlog.css">
}

    <!-- Header Bölümü -->
    <header class="header-bg py-4">
        <div class="container d-flex justify-content-between align-items-center">
            <div>
                <a href="" class="text-white text-decoration-none h3 mb-0 fw-bold">Şevval Blog</a>
                <a href="/" class="btn btn-outline-light btn-sm ms-3 rounded-pill">sevval.com'a Geri Dön</a>
            </div>
            @if (isAdminOrAdditionalAdmin)
            {
                <nav>
                    <a href="@Url.Action("Create", "Blog")" class="btn btn-success admin-btn rounded-pill px-4 py-2 shadow-sm">
                        <i class="bi bi-plus-circle me-2"></i> Yeni Yazı Ekle
                    </a>
                </nav>
            }
        </div>
    </header>

    <main class="container my-5 flex-grow-1 animate-fade-in">
        <div class="blog-details-card">
            <h1 class="blog-details-title">@Model.Title</h1>
            @if (!string.IsNullOrEmpty(Model.ImageUrl))
            {
                <img src="@Model.ImageUrl" alt="@Model.Title" class="blog-details-image">
            }
            else
            {
                <img src="https://placehold.co/1200x600/cccccc/333333?text=G%C3%B6rsel+Yok" alt="Görsel Yok" class="blog-details-image">
            }
            <div class="blog-details-meta">
                <span>Yazar: <span class="font-semibold">@Model.Author</span></span>
                <span>Yayınlanma Tarihi: <span class="font-semibold">@Model.PublishDate.ToShortDateString()</span></span>
                @if (!string.IsNullOrEmpty(Model.Category))
                {
                    <span>Kategori: <span class="font-semibold">@Model.Category</span></span>
                }
            </div>
            <div class="blog-content">
                @Html.Raw(Model.Content.Replace("\n", "<br/>"))
            </div>
            <div class="d-flex justify-content-between items-center mt-6 border-top pt-4 border-gray-200">
                <a href="@Url.Action("Index", "Blog")" class="btn btn-secondary back-btn d-flex align-items-center">
                    <i class="bi bi-arrow-left me-2"></i> Tüm Yazılara Geri Dön
                </a>
                @if (isAdminOrAdditionalAdmin)
                {
                    <div class="d-flex gap-3">
                        <a href="@Url.Action("Edit", "Blog", new { id = Model.Id })" class="btn admin-action-btn edit">
                            Düzenle
                        </a>
                        <a href="@Url.Action("Delete", "Blog", new { id = Model.Id })" class="btn admin-action-btn delete">
                            Sil
                        </a>
                    </div>
                }
            </div>
        </div>
    </main>

    <!-- Footer Bölümü -->
    <footer class="footer-bg text-center">
        <div class="container">
            <p class="mb-0">&copy; @DateTime.Now.Year Şevval Blog. Tüm Hakları Saklıdır.</p>
            <p class="small mt-1">Modern ve İlham Veren Blog Deneyimi.</p>
        </div>
    </footer>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
}
