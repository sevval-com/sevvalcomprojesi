@model (string KullaniciKodu, string Durum)

@{
    ViewData["Title"] = "E-Devlet Başaralı";
    Layout = null; // Layout kullanılmıyor
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-success text-white">
                    <h4 class="mb-0">Doğrulama Başarılı</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-success">
                        <i class="fas fa-check-circle"></i>
                        E-Devlet doğrulamanız başarıyla tamamlanmıştır.
                    </div>
                    <div class="mt-4">
                        <a href="@Url.Action("IlanVer", "Ilan")" class="btn btn-primary">
                            <i class="fas fa-plus"></i> İlan Vermeye Başla
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> 